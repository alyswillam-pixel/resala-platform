services:
  docs:
    image: resala_platform_local_docs
    container_name: resala_platform_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./resala_platform:/app/resala_platform:z
    ports:
      - '9000:9000'
    command: /start-docs
